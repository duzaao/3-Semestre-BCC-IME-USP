#include <bits/stdc++.h>

using namespace std;

int main(){
    stack<int> q;
    q.push(1);


    queue<int> x;
    x.push(3);

    int n;
    cin >> n;
    vector<int> v(n,0);
    for(int i=0;i<v.size();i++)
    cout << v[i] << " ";

    set<int> s;
}